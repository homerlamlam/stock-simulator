# AGENTS.md

## 1. 项目目标

本项目是一个 A 股优先的股票买卖信号辅助系统。

目标：

- 根据行情波动和技术指标生成可解释的交易辅助信号。
- 支持自选股、策略参数调整和模拟盈亏。
- MVP 阶段使用 Mock/延迟行情。
- 后续通过统一数据服务接口接入真实 A 股数据源。

非目标：

- 不做登录。
- 不做数据库。
- 不做真实券商接入。
- 不做自动交易。
- 不承诺盈利。
- 不把系统表述为投资建议。

## 2. 技术栈

- React
- TypeScript
- Vite
- Tailwind CSS
- Recharts
- TanStack Query
- Zustand
- Vitest
- localStorage

依赖原则：

- 不引入不必要依赖。
- 图表统一使用 Recharts。
- 服务端状态统一通过 TanStack Query 管理。
- 本地 UI 和配置状态统一通过 Zustand 管理。
- 用户配置只使用 localStorage，不引入数据库。

## 3. 编码规范

- 代码要适合新手理解。
- 优先清晰，避免过早抽象。
- 每个文件职责单一。
- 函数命名直观。
- 类型定义明确。
- 避免使用 `any`。
- 核心业务逻辑必须使用纯函数。
- 纯函数不能依赖 React、Zustand、TanStack Query 或浏览器环境。
- 对可能失败的操作返回明确结果。
- 不允许核心计算返回 `NaN` 或 `Infinity`。

## 4. 样式规范

- 页面必须响应式。
- 视觉风格年轻、干净、温暖、专业。
- 移动端优先保证可读性。
- 375px、768px、1440px 宽度下不能出现横向滚动。
- 不允许文字和组件重叠。
- 不允许按钮文字溢出。
- 卡片圆角不超过 8px。
- 不做复杂营销型首页，直接展示产品主界面。
- 使用暖白、浅橙、深灰作为基础。
- 涨跌颜色要克制，避免刺眼。
- 不使用大面积紫色、深蓝、棕色或单一色系。
- loading、error、empty 状态必须有明确 UI。
- 信号卡片不能只显示结论，必须显示原因。

## 5. Codex 工作规则

- 每次改代码前先说明计划。
- 每次只完成一个小任务。
- 不要同时改多个无关模块。
- 不要改动无关文件。
- 不要引入不必要依赖。
- 不要提前实现 v2 功能。
- 必要时先查看 `PLAN.md`，确认当前任务对应的步骤、验收标准和状态记录。
- 完成一个计划步骤后，更新 `PLAN.md` 中该步骤的状态、完成时间和备注。
- 遇到需求不明确时，先说明假设或提问。
- 完成后说明修改了哪些文件。
- 完成后说明如何验证。
- 如果测试、lint、build 没有运行，必须明确说明原因。

Mock 数据规则：

- 使用可复现的伪随机生成器。
- 不在业务逻辑中贯穿使用不可控的 `Math.random()`。
- 同一 symbol 在同一 session 内走势应连续。
- 支持 mock error 开关。
- 支持 empty candles、extreme candles、flat candles、uptrend candles、downtrend candles fixture。
- Mock service 可以使用内存状态，但不能写数据库。

Git 和文件规则：

- 不执行破坏性 git 命令。
- 不还原用户已有改动，除非用户明确要求。
- 不删除未知文件。
- 不移动无关文件。
- 不格式化整个项目，除非当前任务明确需要。
