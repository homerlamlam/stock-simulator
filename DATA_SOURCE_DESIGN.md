# 真实行情数据适配层设计

## 目标

真实行情接入必须通过统一的数据服务接口进入应用。UI、store、hooks 不直接依赖具体供应商，也不直接处理供应商字段。

## 接口边界

现有 `MarketDataService` 继续作为唯一入口：

- `searchStocks(keyword, options)`：输入搜索关键字和可选市场，输出归一化后的股票列表。
- `getQuote(symbol)`：输入内部唯一 `symbol`，输出归一化后的当前行情。
- `getCandles(symbol, range)`：输入内部唯一 `symbol` 和周期，输出归一化后的 K 线数组。

真实数据源适配器只负责：

- 调用供应商 API。
- 将供应商字段转换为项目内部类型。
- 将供应商错误转换为项目内部错误状态。
- 在需要时声明行情是否延迟或不可用。

真实数据源适配器不负责：

- 渲染 UI。
- 维护自选股。
- 计算指标。
- 生成买卖信号。
- 读写数据库。
- 自动交易。

## Symbol 归一化规则

内部唯一标识继续使用 `symbol`，展示使用 `displayCode`。

| 市场 | 示例 symbol | displayCode | exchange |
| --- | --- | --- | --- |
| A股 | `600519.SH` | `600519` | `SH` |
| A股 | `000001.SZ` | `000001` | `SZ` |
| A股 | `430047.BJ` | `430047` | `BJ` |
| 港股 | `00700.HK` | `00700` | `HKEX` |
| 美股 | `AAPL.US` | `AAPL` | `NASDAQ` |

UI 只显示 `displayCode`、名称和市场，不显示 `.SH`、`.SZ`、`.HK` 等后缀。跨市场或跨交易所冲突由内部 `symbol` 解决。

## 数据字段归一化

供应商字段必须转换为以下内部类型：

- 股票搜索结果转换为 `StockSymbol`。
- 当前行情转换为 `Quote`。
- K 线转换为 `Candle`。

数值字段要求：

- 价格统一为普通数字。
- 涨跌幅统一为百分比数字，例如 `1.25` 表示 `1.25%`。
- 成交量统一为股数或供应商可解释的最小成交单位。
- 时间统一为 ISO 字符串。
- 无效数值不能向下游返回 `NaN`、`Infinity`。

## 数据源模式

数据源模式分为四类：

- `mock`：默认模式，只使用本地 Mock 行情。
- `real`：只使用真实 API；未配置时不发起请求，直接返回未配置状态。
- `hybrid`：优先真实 API，失败时回退到 Mock，并显示回退提示。
- `unconfigured`：真实 API 缺少必要配置。

MVP 默认保持 `mock`，避免未配置真实 API 时影响页面可用性。

## 错误状态

真实数据源错误统一映射为以下状态：

- `not_configured`：缺少 API key 或供应商配置。
- `network_error`：网络不可用或请求失败。
- `rate_limited`：供应商限流。
- `unauthorized`：API key 无效或无权限。
- `delayed`：当前数据为延迟行情。
- `market_closed`：市场休市或当前无最新交易。
- `provider_error`：供应商返回不可恢复错误。

错误状态必须包含用户可读文案，并保留原始错误的简短调试信息供开发阶段排查。

## 降级策略

- `mock` 模式不访问真实 API。
- `real` 模式未配置时显示未配置状态，不自动静默回退。
- `hybrid` 模式真实 API 失败时回退到 Mock，并在 UI 显示当前正在使用 Mock 回退数据。
- 搜索、Quote、Candles 可以分别降级，不要求全部同时成功。
- 任何真实数据源失败都不能导致白屏，已有本地交互必须继续可用。
